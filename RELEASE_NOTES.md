# DeepSeek Harness Launcher v0.2.0

**首个公开发布版本。**

DeepSeek Harness（dsh）是在本地运行的 AI 编程助手工作台。本启动器把它从「命令行 + 浏览器」变成真正的 macOS 桌面应用：双击启动，原生窗口内体验，退出即清理，不留后台进程。

## 亮点

- **一键运行**：无需熟悉终端，双击即用。启动器自动拉起 dsh 本地服务（`127.0.0.1:3080`），服务就绪后自动进入界面
- **原生体验**：界面内嵌 WKWebView（与 Safari 同源），不依赖 Chrome；持久化存储保留登录态
- **干净退出**：关闭窗口 / Cmd+Q 自动终止服务并兜底清理占用端口，无僵尸进程残留
- **一个 App 通吃两种芯片**：Universal 二进制，同时支持 Intel（x86_64）与 Apple Silicon（arm64）

## 主要特性

| 特性 | 说明 |
|------|------|
| 开箱即用 | DMG 拖入 Applications 即安装；首次启动自动拉取并运行 dsh 服务 |
| 启动等待画面 | 服务就绪前显示加载动画，120s 超时，失败可一键重试 |
| 端口冲突兜底 | 检测到 3080 已被占用时直接复用已有服务，不重复启动 |
| 依赖预检 | 启动前检测 Node.js / pnpm 是否就绪，缺失时给出明确提示 |
| 站外链接外投 | 点击 `127.0.0.1` 之外的链接自动交给系统默认浏览器 |
| 完整日志 | 运行事件写入 `/tmp/dsh-launcher.log`（北京时间）；页面内 JS 的 console 日志与报错同步转发到该日志，排查问题无需开开发者工具 |
| 附件下载 | 支持 Session 导出等文件直接保存到 `~/Downloads`（WKDownload） |
| 实时版本 | 「关于」窗口显示 dsh 启动后实际运行的版本，避免版本漂移 |
| 旧系统兜底 | 内置 Web API polyfill，为旧版 WebKit 补齐缺失能力 |

## 安装

1. 下载 DMG：`dsh-app-0.2.0-universal.dmg`（Intel 与 Apple Silicon 通用，无需区分架构）
2. 拖入 `/Applications`
3. 双击运行，首次启动会自动下载并启动 dsh 服务，请保持网络畅通

## 环境要求

| 依赖 | 要求 |
|------|------|
| macOS | 12.0 及以上；**推荐 macOS 26**——内嵌 WebKit 渲染能力随系统版本走，新系统下页面 Web 能力最完整 |
| Node.js | 20 及以上（推荐 24 LTS） |
| pnpm | 10 及以上（启动器通过 `pnpm dlx` 拉起 dsh） |

## 已知限制

- 首次启动需通过 `pnpm dlx` 联网拉取 dsh 包，等待时间取决于网络（最长 120s）
- 页面的 Web 能力依赖系统 WebKit 版本：macOS 12 对应的引擎较旧，部分新特性不可用（已通过内置 polyfill 兜底关键 API）
- 当前仅支持 macOS

## 反馈

发现任何问题，欢迎提交 Issue（附上日志更有助于定位：`/tmp/dsh-launcher.log`）。